{
  "routes": [
    {
      "method": "GET",
      "path": "/banner-images",
      "handler": "banner-image.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/banner-images/count",
      "handler": "banner-image.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/banner-images/:id",
      "handler": "banner-image.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/banner-images",
      "handler": "banner-image.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/banner-images/:id",
      "handler": "banner-image.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/banner-images/:id",
      "handler": "banner-image.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
